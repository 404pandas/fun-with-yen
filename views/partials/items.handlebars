<a
  class='carousel-item saved-click'
  onclick="routeToHome('{{name}}', '{{file_path}}')"
>
  <div class='container dropdown'>
    <div class='item-container'>
      <h6>{{name}}</h6>
      <img src={{file_path}} />
    </div>
  </div>
</a>